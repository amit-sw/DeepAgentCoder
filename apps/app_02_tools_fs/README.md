# App 02 - Tools + Filesystem

Goal: add filesystem tools so the agent can create or edit files.

## Run
```bash
python apps/app_02_tools_fs/main.py
```

## Notes
- Filesystem tools are enabled via `FilesystemBackend`.
- The agent should use paths rooted at `/`.
