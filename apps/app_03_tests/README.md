# App 03 - Tests + Retry

Goal: add a test runner tool and encourage a fix-and-retry loop.

## Run
```bash
python apps/app_03_tests/main.py
```

## Notes
- The test tool runs `pytest` by default.
- Use `DEEPAGENT_MODEL` to pick your model in the environment.
